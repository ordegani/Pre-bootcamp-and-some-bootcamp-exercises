//בחירת מילה רנדומלית והדפסתה בכוכביות
var items = ['apple', 'grape', 'orange'];
var word = items[Math.floor(Math.random() * items.length)];
word.split("");
let word2 = word.replace(/./g, '*');

//ניחוש אות
const input = require('readline-sync');
let guess = input.question("Please choose a letter: ");
var remainingLetters = word.length;
var answerArray = [];
for (var i = 0; i < word.length; i++) {
answerArray[i] = "_";}

for (var j = 0; j < word.length; j++) {
    if (word[j] === guess) {
        word2[j] = guess;
        remainingLetters--;}}
        console.log(answerArray.join(" "));
    //let word2 = (word.replace(/./g, '*'));
   //word = item1.replace(item1[2],guess);
   //console.log(word);
//for (var i = 0; i <guess[i]; i++)
  //  console.log(guess[i]);
//}}

     //word[j] = guess;
     //let char = word.charAt(j);
     
     //if (item.includes(letter)){
   // const indexOfFirst = item.indexOf(letter);
    //console.log(indexOfFirst);
          
    //let item2 = item1.replace(letter(index), letter);
          //console.log(item2);

    //console.log(item1.replace('*',letter));}

